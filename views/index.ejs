<%- include('header'); -%>
<section id='wrapper'>
    <h1>LacList</h1>
    <% item.forEach(element => { %>
    <form action="/" method="post">
        <div class='container'>
            <div class='left'>
                <input type="checkbox" name="removeItem" value="<%=element.food%>">
                <span><%=element.food%></span>
            </div>
            <div class="right">
                <% if (element.hour === 0) { %>
                <span class='left'>12 am</span>
                <% } else if (element.hour < 12){ %>
                <span class='left'><%=element.hour%> am</span>
                <% } else { %>
                <span class='left'><%=element.hour - 12%> pm</span>
                <% } %>
                <span class="right"><%=element.day%></span>
            </div>
        </div>
        <% }); %>
        <% if (Object.keys(item).length === 0) { %>
        <form action="/" method="post">
            <% } %>
            <div class='itemItemContainer'>
                <input type="text" name="newItem" id='itemInput' placeholder="What went down to your stomach?"
                    autofocus>
            </div>
            <button type="submit" name='operation' value="true">Add</button>
            <button type="submit" name='operation' value="false"><i class="fas fa-minus-circle"></i></button>
        </form>
</section>

<%- include('footer'); -%>