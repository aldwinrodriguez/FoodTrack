<%- include('header'); -%>
<section id='wrapper'>
    <h1>LacList</h1>
    <% item.forEach(element => { %>
    <form action="/" method="post">
        <div class='container'>
            <div class='left'>
                <input type="checkbox" class="checkboxItem" name="removeItem" value="<%=element.food%>">
                <span><%=element.food%></span>
            </div>
            <div class="right">
                <% if (element.hour === 0) { %>
                <span class='left'>12 am</span>
                <% } else if (element.hour < 12){ %>
                <span class='left'><%=element.hour%> am</span>
                <% } else { %>
                <span class='left'><%=element.hour - 12%> pm</span>
                <% } %>
                <span class="right"><%=element.day%></span>
            </div>
        </div>
        <% }); %>
        <% if (Object.keys(item).length === 0) { %>
        <form action="/" method="post">
            <% } %>
            <div class='itemItemContainer'>
                <input type="text" name="newItem" id='itemInput' placeholder="What went down to your stomach?"
                    autofocus>
            </div>
            <div class='buttonContainer'>
                <button type="submit" id='add' name='operation' value="true"><i class="fas fa-plus-circle"></i></button>
                <button type="submit" id='remove' name='operation' class="hide" value="false"><i class="fas fa-minus-circle"></i></button>
            </div>
        </form>
</section>

<%- include('footer'); -%>